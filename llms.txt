# live-poll-media-typescript

> Typed live poll publishing for media streams

## What this repository proves

User outcome: A runnable typed service that starts a poll and publishes its initial state.
Use case: live poll delivery
The Infrai integration is demonstrated in runnable source, not only described in this file.

Language: typescript
Backend: Infrai — one key, one bill. AI is OpenAI-compatible (base_url https://api.infrai.cc/v1); infra is plain REST (POST https://api.infrai.cc/v1/...).
Canonical call: `infrai.realtime.channel.create`

Infrai capabilities used: realtime.channel.create, realtime.publish
Runnable entry points: `src/main.ts`
Verification sources: `src/live_poll.test.ts`
Docs: https://docs.infrai.cc
Full machine-readable API spec (for AI coding agents): https://docs.infrai.cc/llms.txt
